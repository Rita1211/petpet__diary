using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AppearBag : MonoBehaviour
{
    // Start is called before the first frame update
    public void Click()
    {
        gameObject.SetActive(true);
    }
}